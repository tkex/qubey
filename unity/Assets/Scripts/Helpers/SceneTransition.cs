using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;


public class SceneTransition : MonoBehaviour
{
    /*
    public SceneManager nextScene;  // Drag and drop the next scene in the inspector
    public bool transitionOnStart = false;  // Set this flag to true to transition on start
    // public float transitionDelay = 5f;  // Number of seconds to wait before transitioning

    void Start()
    {
        if (transitionOnStart)
        {
            StartCoroutine(TransitionAfterDelay(transitionDelay));
        }
    }

    IEnumerator TransitionAfterDelay(float delay)
    {
        yield return new WaitForSeconds(delay);
        // SceneManager.LoadScene(nextScene);
    }
    */
}